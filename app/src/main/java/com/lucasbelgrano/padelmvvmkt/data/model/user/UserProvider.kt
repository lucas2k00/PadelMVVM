package com.lucasbelgrano.padelmvvmkt.data.model.user

class UserProvider {
    private val users = listOf(
        UserModel(
            user = "test",
            password = "test"
        )
    )
}